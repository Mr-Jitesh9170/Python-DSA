#1 declaring class 
#i)create class in python using `class` keyword.
#ii)class has three components=> propertiese, constructor and methods.

# Explaning components of class.
# i)Properties=> variables are properties.
# ii)constructor=> _init_( ) , it is a constructor(method) to initialise object.
# iii)methods=> it used to perporm certain task and can be re-used for an object.
  
class Parent:
    x=5;
    y=10;
    
print(Parent().x)